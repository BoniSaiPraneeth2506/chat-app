# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
# Local email previews from scripts/digest.js --preview
.email-preview/

# Firebase Admin service-account keys (contain the private key — never commit).
# They are loaded via env vars / Render secrets, not from these files.
backend/firebase-service-account.json
*firebase-adminsdk*.json
*-firebase-adminsdk*.json

